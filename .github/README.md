# tree-sitter-jass

A Tree-sitter grammar for the JASS scripting language used by Warcraft III.

## Features

* Full JASS grammar
* Incremental parsing through Tree-sitter
* Generated C parser
* Node.js bindings
* Java bindings
* Query files for syntax-aware tooling
* Suitable for editors, language servers, linters, and static analysis

## Project Structure

```text
grammar.js
grammar-defs.js
src/
bindings/c/
bindings/node/
bindings/java/
```

## Scope

This repository focuses on parsing and syntax tree generation.

Semantic validation is intentionally outside the scope of the grammar.

Examples of checks that are not performed:

* Undefined symbols
* Invalid function calls
* Type mismatches
* Incorrect native usage
* Warcraft III API compatibility checks

A source file may therefore parse successfully and still fail compilation.

## Source Encoding

JASS source files are traditionally ANSI-encoded (single-byte character encoding).

This grammar operates on text supplied by the host application and does not enforce a specific code page. However, for compatibility with Warcraft III source files, the following assumptions apply:

* ANSI-encoded source files are supported.
* A UTF-8 BOM (`EF BB BF`) at the beginning of the file is tolerated.
* The byte values `0x00` and `0xFF` are invalid anywhere in the source file, including inside comments and string literals.

Files containing `0x00` or `0xFF` are considered malformed and are not valid JASS source files.

## Semantic Analysis

For semantic validation and advanced static analysis, see Prism:

https://github.com/Slayer95/prism

Prism consumes the trees generated by this grammar and performs:

* Symbol resolution
* Type checking
* Semantic diagnostics
* Linting
* Additional language tooling

## Node.js

```bash
npm install github:Slayer95/tree-sitter-jass#main
```

Ssource files should typically be loaded using the `latin1` encoding:

```javascript
const Parser = require('tree-sitter');
const JASS = require('tree-sitter-jass');

const parser = new Parser();
parser.setLanguage(JASS);

const sourceCode = fs.readFileSync('war3map.j', 'latin1');
const tree = parser.parse(sourceCode);
```

## Java

Java bindings are included in this repository for consumers embedding the parser into JVM-based tooling.

Refer to the files under:

```text
bindings/java/
```

## Development

Generate the parser:

```bash
tree-sitter generate
```

## License

MIT
